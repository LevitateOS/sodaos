[Socket]
ListenStream=
ListenStream=9090
# Operator LAN access with the native root password; the SSH tunnel remains
# available. The operator restricts this deliberately; never publish it
# beyond the private network.
