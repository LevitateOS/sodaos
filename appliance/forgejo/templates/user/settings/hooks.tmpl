{{/* Stock Forgejo 15.0.7 content; artwork is scoped to the personal webhook list. */}}
{{template "user/settings/layout_head" (dict "ctxData" . "pageClass" "user settings webhooks" "title" (ctx.Locale.Tr "repo.settings.hooks"))}}
	<div class="user-setting-content">
		{{template "repo/settings/webhook/base_list" (dict "SettingsPresentation" true "ctxData" .)}}
{{template "repo/settings/webhook/delete_modal" .}}
	</div>
{{template "user/settings/layout_footer" .}}
