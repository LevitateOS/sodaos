[containers]
# Workloads are already inside the project's delegated user namespace.
userns = "host"
cgroups = "disabled"

[engine]
cgroup_manager = "cgroupfs"
events_logger = "file"
compose_providers = ["/usr/local/bin/podman-compose"]
